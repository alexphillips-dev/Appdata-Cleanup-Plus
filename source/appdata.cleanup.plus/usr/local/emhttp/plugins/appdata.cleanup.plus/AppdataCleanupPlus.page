Menu="Utilities"
Title="Appdata Cleanup Plus"
Icon="appdata.cleanup.plus.png"
Type="xmenu"
---
<?php
require_once("/usr/local/emhttp/plugins/appdata.cleanup.plus/include/helpers.php");

$cssVersion = @filemtime("/usr/local/emhttp/plugins/appdata.cleanup.plus/styles/appdata.cleanup.plus.css");
$scriptVersions = array_filter(array(
  @filemtime("/usr/local/emhttp/plugins/appdata.cleanup.plus/scripts/appdata.cleanup.plus.shared.js"),
  @filemtime("/usr/local/emhttp/plugins/appdata.cleanup.plus/scripts/appdata.cleanup.plus.panels.js"),
  @filemtime("/usr/local/emhttp/plugins/appdata.cleanup.plus/scripts/appdata.cleanup.plus.js")
));
$jsVersion = ! empty($scriptVersions) ? max($scriptVersions) : false;
$hostThemeName = strtolower(trim((string)($display['theme'] ?? "")));
$hostThemeClass = "";
$csrfToken = getAppdataCleanupPlusCsrfToken();
$pluginVersion = "";
$manifestPaths = array(
  "/boot/config/plugins/appdata.cleanup.plus.plg",
  appdataCleanupPlusConfigDir() . "/appdata.cleanup.plus.plg"
);

foreach ( $manifestPaths as $manifestPath ) {
  if ( ! is_file($manifestPath) ) {
    continue;
  }

  $manifestContents = @file_get_contents($manifestPath);
  if ( is_string($manifestContents) && preg_match('/<!ENTITY version "([^"]+)">/', $manifestContents, $versionMatches) ) {
    $pluginVersion = (string)$versionMatches[1];
    break;
  }
}

if ( $hostThemeName === "grey" ) {
  $hostThemeName = "gray";
}

if (
  strpos($hostThemeName, "white") !== false ||
  strpos($hostThemeName, "light") !== false ||
  strpos($hostThemeName, "azure") !== false
) {
  $hostThemeClass = "light";
} elseif ( strpos($hostThemeName, "black") !== false || strpos($hostThemeName, "gray") !== false ) {
  $hostThemeClass = "dark";
}

if ( ! $cssVersion ) {
  $cssVersion = time();
}

if ( ! $jsVersion ) {
  $jsVersion = time();
}

$config = array(
  "plurals" => acpPluralData(),
  "locale" => acpLocale(),
  "languageTag" => acpLocales()[acpLocale()]["tag"],
  "direction" => !empty(acpLocales()[acpLocale()]["rtl"]) ? "rtl" : "ltr",
  "timeZone" => date_default_timezone_get(),
  "catalog" => acpCatalog(),
  "apiUrl" => "/plugins/appdata.cleanup.plus/include/exec.php",
  "csrfToken" => $csrfToken,
  "spinnerUrl" => "/plugins/appdata.cleanup.plus/images/spinner2.gif",
  "logoUrl" => "/plugins/appdata.cleanup.plus/images/appdata.cleanup.plus.png",
  "pluginVersion" => $pluginVersion,
  "strings" => array(
    "loadingTitle" => acpT("Scanning appdata sources and saved Docker templates"),
    "loadingMessage" => acpT("Reviewing orphaned appdata folders and active container mappings."),
    "emptyTitle" => acpT("No orphaned appdata found"),
    "emptyMessage" => acpT("Nothing currently looks safe to clean up from the configured appdata sources or saved Docker templates."),
    "emptyNoUnusedFoundMessage" => acpT("No unused appdata folders were found under the current scan paths."),
    "emptyScannedSinglePrefix" => acpT("Scanned appdata root:"),
    "emptyScannedPluralPrefix" => acpT("Scanned appdata roots:"),
    "emptyNoRootsMessage" => acpT("No appdata source roots are configured right now."),
    "emptyTemplatesCheckedMessage" => acpT("Saved Docker templates were checked too."),
    "emptyDockerOfflineCheckedMessage" => acpT("Docker is offline, so only saved Docker templates could be checked."),
    "emptyTemplateEducationMessage" => acpT("Saved Docker templates can still reference appdata after a container is removed, so they are included in the scan evidence."),
    "emptyNextStepsMessage" => acpT("If you expected orphaned folders, confirm the full appdata root in Appdata sources, remove the container first, and rescan."),
    "noMatchesTitle" => acpT("No folders match your search"),
    "noMatchesMessage" => acpT("Clear the search to see the full scan again."),
    "ignoredOnlyTitle" => acpT("Only ignored paths remain"),
    "ignoredOnlyMessage" => acpT("Every detected candidate is hidden by your ignore list. Turn on Show ignored to review or restore hidden paths."),
    "scanFailedTitle" => acpT("Scan failed"),
    "scanFailedMessage" => acpT("The orphaned appdata scan could not be completed right now."),
    "backendBusyTitle" => acpT("Backend busy"),
    "backendBusyMessage" => acpT("Another Appdata Cleanup Plus operation is still running. Wait a few seconds, then try again."),
    "scanReadOnlyTitle" => acpT("Read-only scan"),
    "scanReadOnlyMessage" => acpT("Scan results loaded, but actions are disabled because a secure snapshot could not be created right now."),
    "scanExpiredMessage" => acpT("This scan expired or is no longer valid. Rescan and try again."),
    "rescanLabel" => acpT("Rescan"),
    "selectAllLabel" => acpT("Select all"),
    "clearLabel" => acpT("Clear"),
    "deleteEmptyTitle" => acpT("Nothing selected"),
    "deleteEmptyMessage" => acpT("Select at least one deletable folder before continuing."),
    "deleteConfirmTitle" => acpT("Delete selected folders?"),
    "deleteConfirmMessage" => acpT("Selected folders will be removed immediately."),
    "deleteConfirmButton" => acpT("Delete"),
    "quarantineConfirmTitle" => acpT("Quarantine selected folders?"),
    "quarantineConfirmMessage" => acpT("Selected folders will be moved into quarantine instead of being permanently deleted."),
    "quarantineConfirmButton" => acpT("Quarantine"),
    "deleteWarningLabel" => acpT("WARNING"),
    "previewWarningLabel" => acpT("DRY RUN"),
    "deleteIrreversibleMessage" => acpT("This action cannot be undone by this plugin."),
    "deleteListTitle" => acpT("Folders to delete"),
    "deleteReviewLabel" => acpT("Review required"),
    "deleteSafeLabel" => acpT("Ready"),
    "deleteReviewCountLabel" => acpT("Review"),
    "deleteFolderSingular" => acpT("folder"),
    "deleteFolderPlural" => acpT("folders"),
    "deletePreviewLoadingTitle" => acpT("Preparing delete review"),
    "deletePreviewLoadingMessage" => acpT("Checking the selected items and collecting any recursive ZFS delete details."),
    "deletePreviewFailedMessage" => acpT("The delete preview could not be prepared right now."),
    "dryRunLabel" => acpT("Dry run"),
    "dryRunTitle" => acpT("Running dry run"),
    "dryRunMessage" => acpT("Reviewing what the current action would do without changing anything."),
    "deletingTitle" => acpT("Deleting selected folders"),
    "deletingMessage" => acpT("Large folders can take a moment to remove."),
    "deleteProgressTitle" => acpT("Deleting folders"),
    "deleteProgressPreparing" => acpT("Preparing delete progress."),
    "deleteProgressRunning" => acpT("Removing selected folders now."),
    "deleteProgressComplete" => acpT("Delete finished. Review the results before leaving this screen."),
    "deleteProgressCurrentLabel" => acpT("Current"),
    "deleteProgressViewResults" => acpT("View results"),
    "quarantiningTitle" => acpT("Moving selected folders to quarantine"),
    "quarantiningMessage" => acpT("Selected folders are being moved into a hidden quarantine root."),
    "deleteResultTitleSuccess" => acpT("Cleanup complete"),
    "deleteResultTitleWarning" => acpT("Cleanup finished with warnings"),
    "deleteFailedTitle" => acpT("Delete failed"),
    "quarantineResultTitleSuccess" => acpT("Quarantine complete"),
    "quarantineResultTitleWarning" => acpT("Quarantine finished with warnings"),
    "quarantineFailedTitle" => acpT("Quarantine failed"),
    "quarantineLoadFailedMessage" => acpT("The quarantine manager could not be loaded right now."),
    "dryRunResultTitleSuccess" => acpT("Dry run complete"),
    "dryRunResultTitleWarning" => acpT("Dry run finished with warnings"),
    "ignoreActionLabel" => acpT("Ignore"),
    "restoreActionLabel" => acpT("Restore"),
    "ignoreFailedTitle" => acpT("Ignore failed"),
    "ignoreFailedMessage" => acpT("The ignore list could not be updated right now."),
    "restoreFailedTitle" => acpT("Restore failed"),
    "restoreFailedMessage" => acpT("The ignore list could not be updated right now."),
    "settingsSaveFailedTitle" => acpT("Safety settings failed"),
    "settingsSaveFailedMessage" => acpT("The new safety settings could not be saved right now."),
    "helpOpenLabel" => acpT("Help"),
    "helpTitle" => acpT("Help"),
    "helpSubtitle" => acpT("A quick guide for reviewing and cleaning unused appdata without digging through technical details."),
    "helpIntroTitle" => acpT("Clean up appdata safely"),
    "helpFlowTitle" => acpT("Simple workflow"),
    "helpStepReviewTitle" => acpT("Review"),
    "helpStepReviewBody" => acpT("Ready rows look unused. Open Details only when a folder name, path, or source badge is unclear."),
    "helpStepPreviewTitle" => acpT("Preview"),
    "helpStepPreviewBody" => acpT("Use Dry run to see what would happen before anything is changed."),
    "helpStepCleanTitle" => acpT("Clean"),
    "helpStepCleanBody" => acpT("Select ready rows, then quarantine or delete them depending on Safe Mode."),
    "helpSafeModeSimpleTitle" => acpT("Safe Mode"),
    "helpSafeModeSimpleBody" => acpT("Safe Mode moves folders to quarantine. Disable it only when you intentionally want permanent delete."),
    "helpZfsSimpleTitle" => acpT("ZFS datasets"),
    "helpZfsSimpleBody" => acpT("Exact ZFS dataset rows cannot be quarantined. Disable Safe Mode before acting on those rows."),
    "helpDetailsSimpleTitle" => acpT("Need more info?"),
    "helpDetailsSimpleBody" => acpT("Use a row's Details button for the short reason it appeared and what the cleanup action will do."),
    "helpOverviewTitle" => acpT("Overview"),
    "helpOverviewBody" => acpT("Appdata Cleanup Plus finds Docker appdata folders that appear unused. It compares configured appdata scan paths, saved Docker templates, installed container mappings, ignore entries, Safe Mode, and storage details. Safe Mode is the default action and moves selected folders to quarantine; explicitly enable permanent delete mode when irreversible removal is intended."),
    "helpScannedTitle" => acpT("What gets scanned"),
    "helpScannedBody" => acpT("The plugin scans the detected Docker appdata path first, then any added appdata scan paths. It also reads saved Docker templates and installed container paths so template-only folders and currently used folders can be separated. Direct child folders under each scan path are treated as candidates."),
    "helpStatesTitle" => acpT("Result states"),
    "helpStatesBody" => acpT("Ready rows look unused and can be selected. Safety-blocked rows cannot be cleaned here. Ignored rows are hidden until restored."),
    "helpWhyRowsTitle" => acpT("Why rows appear"),
    "helpWhyRowsBody" => acpT("Rows may appear because a saved template still points to the folder, because a folder exists directly inside a scan path with no installed container using it, because Docker is offline and live mappings cannot be verified, or because a configured ZFS mapping matched the path."),
    "helpActionsTitle" => acpT("Actions"),
    "helpActionsBody" => acpT("Details explains the row evidence. Select ready chooses rows that are currently actionable. Ignore hides a row from normal results. Dry run previews what would happen. Delete selected permanently removes folders after confirmation. Safe Mode changes the primary action to quarantine instead."),
    "helpOptionsTitle" => acpT("Safe Mode"),
    "helpOptionsBody" => acpT("Safe Mode changes the main action from permanent delete to quarantine. Exact ZFS dataset rows are enabled by default and use dataset destroy when permanent delete mode is active."),
    "helpQuarantineTitle" => acpT("Quarantine"),
    "helpQuarantineBody" => acpT("Quarantined folders are moved into the configured quarantine root and tracked by this plugin. You can restore them, purge them permanently, or set purge timing. If the original restore path already exists, the restore conflict flow lets you skip the conflict or restore with an edited folder name."),
    "helpZfsTitle" => acpT("ZFS rows"),
    "helpZfsBody" => acpT("Exact ZFS dataset-backed appdata rows cannot be quarantined as normal folders. They require permanent delete mode and ZFS dataset delete, then use dataset destroy. Details shows the dataset name, mountpoint, destroy mode, child datasets, snapshots, and mapping evidence when available."),
    "helpSafetyTitle" => acpT("Safety rules"),
    "helpSafetyBody" => acpT("Safety-blocked rows include share roots, mount roots, symlinked paths, managed Docker or VM storage paths, paths that changed since the scan, missing paths, and unsafe canonical paths. These are blocked at action time too, even if the browser state is stale."),
    "helpTroubleshootingTitle" => acpT("Common questions"),
    "helpTroubleshootingBody" => acpT("If a row is locked, open Details and read Verdict and Action gate. If no rows appear, confirm Appdata sources and rescan. If Unraid does not pull an update, verify the raw dev manifest version and package MD5."),
    "helpWorkflowTitle" => acpT("Recommended workflow"),
    "helpWorkflowBody" => acpT("Rescan, review Ready and Blocked rows, open Details for anything unclear, enable Safe Mode if you want a restorable quarantine step, and permanently delete only when you are confident."),
    "enablePermanentDeleteLabel" => acpT("Permanent delete mode"),
    "enableSafeModeLabel" => acpT("Enable Safe Mode"),
    "disabledLabel" => acpT("Disabled"),
    "sourceLabel" => acpT("Source"),
    "storageLabel" => acpT("Storage"),
    "zfsDatasetLabel" => acpT("ZFS dataset"),
    "zfsRecursiveDestroyLabel" => acpT("Destroy mode"),
    "zfsRecursiveDestroyValue" => acpT("Recursive destroy"),
    "zfsStandardDestroyValue" => acpT("Standard destroy"),
    "zfsDestroyImpactLabel" => acpT("Impact"),
    "zfsChildDatasetsLabel" => acpT("Child datasets"),
    "zfsSnapshotsLabel" => acpT("Snapshots"),
    "readySectionTitle" => acpT("Ready to clean"),
    "readySectionMessage" => acpT("These look like orphaned appdata folders inside configured appdata roots and can be cleaned safely."),
    "protectedSectionTitle" => acpT("Blocked"),
    "protectedSectionMessage" => acpT("Safety-blocked rows cannot be unlocked here."),
    "templateSectionTitle" => acpT("Saved template references"),
    "templateSectionMessage" => acpT("Paths still referenced by saved Docker templates but not by installed containers."),
    "discoverySectionTitle" => acpT("Appdata source discovery"),
    "discoverySectionMessage" => acpT("Folders found directly in the configured appdata sources with no saved template or live container reference."),
    "ignoredSectionTitle" => acpT("Ignored"),
    "ignoredSectionMessage" => acpT("Hidden by your ignore list. Restore them to include them in cleanup results again."),
    "templateLabel" => acpT("Template"),
    "targetLabel" => acpT("Target"),
    "destinationLabel" => acpT("Destination"),
    "sizeLabel" => acpT("Size"),
    "sizeLoadingLabel" => acpT("Loading..."),
    "updatedLabel" => acpT("Updated"),
    "ignoredHiddenSummary" => acpT("ignored hidden"),
    "ignoredShownSummary" => acpT("ignored shown"),
    "badgeReasonOutsideShare" => acpT("Outside share"),
    "badgeReasonOverrideActive" => acpT("Override active"),
    "badgeReasonSymlink" => acpT("Symlink"),
    "badgeReasonMountPoint" => acpT("Mount point"),
    "badgeReasonRootPath" => acpT("Root path"),
    "badgeReasonUnsafePath" => acpT("Unsafe path"),
    "badgeReasonSafetyLock" => acpT("Safety lock"),
    "badgeReasonPolicyLock" => acpT("Policy lock"),
    "badgeReasonTemplateReference" => acpT("Template reference"),
    "badgeReasonDeleteModeOff" => acpT("Delete mode off"),
    "badgeReasonNoExactDataset" => acpT("No exact dataset"),
    "badgeStorageZfs" => acpT("ZFS dataset"),
    "cardDeletable" => acpT("Ready"),
    "selectedSingular" => acpT("folder selected"),
    "selectedPlural" => acpT("folders selected"),
    "selectionPresetLabel" => acpT("Select preset"),
    "selectionPresetApplyLabel" => acpT("Select"),
    "selectionPresetSafeLabel" => acpT("Ready rows"),
    "selectionPresetDiscoveryLabel" => acpT("Discovery only"),
    "selectionPresetZfsLabel" => acpT("ZFS-backed"),
    "selectionPresetOlderLabel" => acpT("Older than 90 days"),
    "selectionPresetEmptyTitle" => acpT("Nothing matched"),
    "selectionPresetEmptyMessage" => acpT("No selectable rows matched that preset in the current scan."),
    "selectionHintIdle" => acpT("Select ready rows to clean them. Protected paths stay blocked."),
    "selectionHintReadOnly" => acpT("Scan results are read-only right now because the secure action snapshot could not be created."),
    "selectionHintDeleteMode" => acpT("Permanent delete requires confirmation with a checkbox."),
    "selectionHintQuarantineMode" => acpT("Selected folders will be moved into quarantine instead of being permanently deleted."),
    "selectionHintZfsMode" => acpT("ZFS dataset-backed rows require permanent delete mode and cannot be quarantined."),
    "noticeSafeModeTitle" => acpT("Safe Mode is ON"),
    "noticeSafeModeMessage" => acpT("Selected folders will be quarantined instead of permanently deleted."),
    "noticeDeleteModeTitle" => acpT("Safe Mode is OFF"),
    "noticeDeleteModeMessage" => acpT("Selected folders will be permanently deleted after confirmation."),
    "noticeDockerOfflineTitle" => acpT("Docker is offline"),
    "noticeDockerOfflineMessage" => acpT("Results are based only on saved Docker templates. Review every path before acting on anything."),
    "noticeIgnoredTitle" => acpT("Ignore list active"),
    "showIgnoredLabel" => acpT("Show ignored"),
    "hideIgnoredLabel" => acpT("Hide ignored"),
    "noticeOutsideShareDisabledTitle" => acpT("Review items need confirmation"),
    "noticeOutsideShareDisabledMessage" => acpT("Review paths stay visible and start locked. Unlock them for this scan after checking details."),
    "noticeLockedPathsTitle" => acpT("Protected paths stay blocked"),
    "noticeLockedPathsMessage" => acpT("Any path that resolves to a share root, mount point, symlinked location, managed Docker/VM path, or other unsafe target cannot be unlocked here."),
    "visibleSummary" => acpT("visible"),
    "detectedSummary" => acpT("detected"),
    "cardTotal" => acpT("Detected"),
    "cardBlocked" => acpT("Blocked"),
    "cardBlockedSubtitle" => acpT("Locked or in use"),
    "cardBlockedSafety" => acpT("Blocked for safety"),
    "cardBlockedOptions" => acpT("Blocked by options"),
    "cardSelected" => acpT("Selected"),
    "actionBarTitle" => acpT("Action bar"),
    "toolsOpenLabel" => acpT("Tools"),
    "toolsTitle" => acpT("Tools"),
    "toolsSubtitle" => acpT("Support and maintainer utilities for exporting the current plugin state."),
    "toolsDiagnosticsTitle" => acpT("Export diagnostics"),
    "toolsDiagnosticsMessage" => acpT("Downloads a structured troubleshooting bundle with health checks, state-file validity and permissions, runtime locks, PHP limits, scan timing, quarantine health, current UI state, and bounded support-log excerpts."),
    "toolsDiagnosticsPrivacyNote" => acpT("The export uses allowlisted state summaries and export-only aliases. App names, template filenames, full paths, search text, stable row IDs, hosts, network addresses, credentials, and tokens are removed or redacted. Review before sharing."),
    "toolsDiagnosticsExportLabel" => acpT("Download diagnostics"),
    "toolsDiagnosticsEmptyMessage" => acpT("Diagnostics can be exported at any time. Run a fresh scan first when troubleshooting scan results or timing."),
    "toolsDiagnosticsDoneTitle" => acpT("Diagnostics exported"),
    "toolsDiagnosticsDoneMessage" => acpT("The sanitized troubleshooting JSON has been downloaded."),
    "toolsDiagnosticsFailedTitle" => acpT("Diagnostics export failed"),
    "toolsDiagnosticsFailedMessage" => acpT("The diagnostics file could not be created right now."),
    "toolsSupportSummaryTitle" => acpT("Copy support summary"),
    "toolsSupportSummaryMessage" => acpT("Copies a concise support summary with version, scan counts, safety toggles, scan roots, ZFS state, and quarantine totals for forum posts or issue reports."),
    "toolsSupportSummaryCopyLabel" => acpT("Copy support summary"),
    "toolsDiagnosticsCopyLabel" => acpT("Copy diagnostics text"),
    "toolsDiagnosticsCopyMessage" => acpT("Copies a redacted plain-text report with health findings, scan timing, runtime locks, collector limits, and up to 15 relevant log lines. Use it for forum posts or when JSON download is unavailable."),
    "toolsDiagnosticsCopyDoneTitle" => acpT("Diagnostics text copied"),
    "toolsDiagnosticsCopyDoneMessage" => acpT("The diagnostics text has been copied to the clipboard."),
    "toolsDiagnosticsCopyFailedTitle" => acpT("Diagnostics text failed"),
    "toolsDiagnosticsCopyFailedMessage" => acpT("The diagnostics text could not be copied right now."),
    "toolsSupportSummaryDoneTitle" => acpT("Support summary copied"),
    "toolsSupportSummaryDoneMessage" => acpT("The support summary has been copied to the clipboard."),
    "toolsSupportSummaryFailedTitle" => acpT("Support summary failed"),
    "toolsSupportSummaryFailedMessage" => acpT("The support summary could not be copied right now."),
    "toolsFixtureTitle" => acpT("Test fixtures"),
    "toolsFixtureSimpleMessage" => acpT("Create safe, namespaced appdata test folders so scan, selection, dry run, quarantine, and delete can be validated."),
    "toolsFixtureRootLabel" => acpT("Fixture root"),
    "toolsFixtureNoRootLabel" => acpT("No appdata root detected"),
    "toolsFixtureStatusHint" => acpT("Refresh status to check fixture paths."),
    "toolsFixtureExistsLabel" => acpT("Created"),
    "toolsFixtureMissingLabel" => acpT("Missing"),
    "toolsFixtureCreateLabel" => acpT("Create fixtures"),
    "toolsFixtureRemoveLabel" => acpT("Remove fixtures"),
    "toolsFixtureWorkingMessage" => acpT("Working on test fixtures."),
    "toolsFixtureDoneMessage" => acpT("Test fixture action completed."),
    "toolsFixtureFailedMessage" => acpT("The test fixture action could not be completed right now."),
    "rowDetailsActionLabel" => acpT("Details"),
    "rowDetailsTitle" => acpT("Row details"),
    "rowDetailsLoadFailedMessage" => acpT("The latest row details could not be loaded right now."),
    "rowDetailsSummaryTitle" => acpT("Why this row looks the way it does"),
    "rowDetailsExplanationTitle" => acpT("Current explanation"),
    "rowDetailsProtectedExplanationTitle" => acpT("Why this row is blocked for safety"),
    "rowDetailsOptionsBlockedExplanationTitle" => acpT("Why this row needs permanent delete mode"),
    "rowDetailsVerdictTitle" => acpT("Verdict"),
    "rowDetailsWhyExistsTitle" => acpT("Why this row exists"),
    "rowDetailsActionGateTitle" => acpT("Action gate"),
    "rowDetailsPathTitle" => acpT("Paths"),
    "rowDetailsSourceTitle" => acpT("Source evidence"),
    "rowDetailsSafetyTitle" => acpT("Safety and actionability"),
    "rowDetailsStorageTitle" => acpT("Storage"),
    "rowDetailsStatsTitle" => acpT("Stats"),
    "rowDetailsOutcomeLabel" => acpT("Current outcome"),
    "rowDetailsWhyLabel" => acpT("Why"),
    "rowDetailsResolveLabel" => acpT("How to resolve"),
    "rowDetailsNextStepLabel" => acpT("Next step"),
    "rowDetailsUnlockabilityLabel" => acpT("Unlockability"),
    "rowDetailsEvidenceLabel" => acpT("Evidence"),
    "rowDetailsCurrentPathLabel" => acpT("Current path"),
    "rowDetailsRealPathLabel" => acpT("Canonical path"),
    "rowDetailsSourceRootLabel" => acpT("Matched source root"),
    "rowDetailsSourceNamesLabel" => acpT("Source names"),
    "rowDetailsTargetsLabel" => acpT("Targets"),
    "rowDetailsTemplateRefsLabel" => acpT("Template refs"),
    "rowDetailsRiskLabel" => acpT("Risk"),
    "rowDetailsStatusLabel" => acpT("Status"),
    "rowDetailsPolicyLabel" => acpT("Policy"),
    "rowDetailsSecurityLabel" => acpT("Security"),
    "rowDetailsStorageKindLabel" => acpT("Storage kind"),
    "rowDetailsDatasetLabel" => acpT("Dataset"),
    "rowDetailsMountpointLabel" => acpT("Mountpoint"),
    "rowDetailsMatchedShareRootLabel" => acpT("Matched share root"),
    "rowDetailsMatchedDatasetRootLabel" => acpT("Matched dataset root"),
    "rowDetailsResolutionLabel" => acpT("Resolution"),
    "rowDetailsCheckedPathsLabel" => acpT("Checked paths"),
    "rowDetailsDestroyModeLabel" => acpT("Destroy mode"),
    "rowDetailsImpactLabel" => acpT("Destroy impact"),
    "rowDetailsMappingLabel" => acpT("ZFS mapping"),
    "rowDetailsInsideSourceLabel" => acpT("Inside configured source"),
    "rowDetailsInsideShareLabel" => acpT("Inside default share"),
    "rowDetailsShareLabel" => acpT("Share"),
    "rowDetailsDepthLabel" => acpT("Depth"),
    "rowDetailsLoadingLabel" => acpT("Loading latest ZFS detail..."),
    "rowDetailsNoImpactLabel" => acpT("No descendant datasets or snapshots are currently expected."),
    "rowDetailsZfsPreviewPending" => acpT("Open details again if you need a fresh ZFS destroy preview."),
    "rowDetailsOutcomeIgnored" => acpT("Ignored until restored"),
    "rowDetailsOutcomeHardLocked" => acpT("Blocked by safety rules"),
    "rowDetailsOutcomeZfsDisabled" => acpT("Blocked until ZFS dataset delete is enabled"),
    "rowDetailsOutcomePermanentDeleteRequired" => acpT("Blocked until permanent delete mode is enabled"),
    "rowDetailsOutcomeMappedNoExactDataset" => acpT("Mapped share path did not resolve to an exact dataset"),
    "rowDetailsOutcomeZfsReady" => acpT("Ready for ZFS dataset destroy"),
    "rowDetailsOutcomeReady" => acpT("Ready under current safety settings"),
    "rowDetailsNextStepIgnored" => acpT("Use Restore if you want this path to appear in cleanup scans again."),
    "rowDetailsNextStepHardLocked" => acpT("No action is available here. The path resolves to a managed or unsafe target."),
    "rowDetailsNextStepEnablePermanentDelete" => acpT("Disable Safe Mode to use permanent delete. ZFS-backed rows cannot be quarantined."),
    "rowDetailsNextStepMappedNoExactDataset" => acpT("Correct the ZFS mapping so the dataset mount root matches exactly, or continue as a normal folder."),
    "rowDetailsNextStepZfsReady" => acpT("Only permanent delete mode is supported for ZFS-backed rows. Review the destroy impact below before proceeding."),
    "rowDetailsNextStepReady" => acpT("You can quarantine it now, or permanently delete it when Safe Mode is off."),
    "rowDetailsYesLabel" => acpT("Yes"),
    "rowDetailsNoLabel" => acpT("No"),
    "rowDetailsNoneLabel" => acpT("None"),
    "rowExplainIgnored" => acpT("This row is hidden by the ignore list."),
    "rowExplainBlocked" => acpT("This path is protected by a safety rule."),
    "rowExplainPolicyLocked" => acpT("This row is blocked by the current cleanup options."),
    "rowExplainZfsReady" => acpT("This row resolves to an exact ZFS dataset and uses dataset destroy instead of folder quarantine."),
    "rowExplainReady" => acpT("This row is ready under the current cleanup settings."),
    "rowUnlockabilityNever" => acpT("Cannot be unlocked here. Safety locks protect managed paths, root paths, symlinks, mount points, and other unsafe targets."),
    "rowUnlockabilitySettings" => acpT("Not unlockable per scan. Disable Safe Mode if this row requires permanent delete."),
    "rowUnlockabilityReady" => acpT("Actionable under the current settings."),
    "rowUnlockabilityUnavailable" => acpT("No cleanup action is currently available."),
    "operationPreviewLead" => acpT("Review what would happen before running the action."),
    "operationResultsSuccessLead" => acpT("The action completed successfully. Review the final status for each row below."),
    "operationResultsWarningLead" => acpT("The action finished, but some rows were skipped, blocked, missing, or failed. Review the list before leaving this screen."),
    "operationResultsCompletedLabel" => acpT("Completed"),
    "appdataSourcesTitle" => acpT("Appdata sources"),
    "appdataSourcesOpenLabel" => acpT("Appdata sources"),
    "appdataSourcesLead" => acpT("The Docker appdata root is auto-detected when available. Browse to a non-standard appdata location, then add it to the manual source list."),
    "appdataSourcesHint" => acpT("Only add dedicated appdata roots. Direct child folders under each root are treated as discovery candidates."),
    "appdataSourcesDetectedTitle" => acpT("Detected source"),
    "appdataSourcesDetectedEmpty" => acpT("No Docker appdata source is auto-detected right now."),
    "appdataSourcesManualTitle" => acpT("Manual source paths"),
    "appdataSourcesManualHint" => acpT("Select a folder path and add it when you reach the full appdata root."),
    "appdataSourcesManualEmpty" => acpT("No manual appdata source paths have been added yet."),
    "appdataSourcesBrowseUpLabel" => acpT("Up"),
    "appdataSourcesAddCurrentLabel" => acpT("Add selected path"),
    "appdataSourcesRemoveLabel" => acpT("Remove"),
    "appdataSourcesCurrentPathLabel" => acpT("Current path"),
    "appdataSourcesBrowseLoadingMessage" => acpT("Loading folders from the selected path."),
    "appdataSourcesBrowseFailedMessage" => acpT("The selected folder could not be browsed right now."),
    "appdataSourcesBrowseEmptyMessage" => acpT("No child folders are available under the current path."),
    "appdataSourcesAlreadyAddedMessage" => acpT("This path is already in the manual source list."),
    "appdataSourcesReadyMessage" => acpT("This path can be added as a manual appdata source."),
    "appdataSourcesInvalidCurrentMessage" => acpT("This path cannot be added as a manual appdata source yet."),
    "appdataSourcesEffectiveTitle" => acpT("Effective scan roots"),
    "appdataSourcesEffectiveEmpty" => acpT("No appdata sources are currently available."),
    "appdataSourcesSaveLabel" => acpT("Save sources"),
    "appdataSourcesSavingMessage" => acpT("Saving appdata sources and rescanning."),
    "appdataSourcesSaveFailedTitle" => acpT("Appdata sources failed"),
    "appdataSourcesSaveFailedMessage" => acpT("The appdata source list could not be saved right now."),
    "zfsPathMappingsTitle" => acpT("ZFS path mappings"),
    "zfsPathMappingsOpenLabel" => acpT("ZFS mappings"),
    "zfsPathMappingsLead" => acpT("Map the user-share appdata root to the dataset mount root so scan results can resolve exact ZFS datasets safely."),
    "zfsPathMappingsHint" => acpT("Choose the share root first, then the matching dataset mount root. Only exact dataset mountpoint matches become ZFS-backed delete candidates."),
    "zfsPathMappingsExistingTitle" => acpT("Configured mappings"),
    "zfsPathMappingsEmpty" => acpT("No ZFS path mappings have been added yet."),
    "zfsPathMappingsDraftTitle" => acpT("Add mapping"),
    "zfsPathMappingsShareRootLabel" => acpT("Unraid share root"),
    "zfsPathMappingsDatasetRootLabel" => acpT("ZFS dataset mount root"),
    "zfsPathMappingsBrowseShareLabel" => acpT("Browse share root"),
    "zfsPathMappingsBrowseDatasetLabel" => acpT("Browse dataset root"),
    "zfsPathMappingsCurrentTargetLabel" => acpT("Picker target"),
    "zfsPathMappingsUseCurrentLabel" => acpT("Use current path"),
    "zfsPathMappingsSuggestionUseLabel" => acpT("Use suggestion"),
    "zfsPathMappingsSuggestionAppliedMessage" => acpT("Suggested roots loaded into the draft. Save mappings to apply them."),
    "zfsPathMappingsSuggestedTitle" => acpT("Suggested mappings"),
    "zfsPathMappingsSuggestedHint" => acpT("These pairings were inferred from your effective scan roots and detected ZFS mountpoints. Review them before saving."),
    "zfsPathMappingsAddLabel" => acpT("Add mapping"),
    "zfsPathMappingsRemoveLabel" => acpT("Remove"),
    "zfsPathMappingsClearDraftLabel" => acpT("Clear draft"),
    "zfsPathMappingsDraftIncompleteMessage" => acpT("Choose both a share root and a dataset root before adding the mapping."),
    "zfsPathMappingsDuplicateMessage" => acpT("This ZFS path mapping is already configured."),
    "zfsPathMappingsSavingMessage" => acpT("Saving ZFS path mappings and rescanning."),
    "zfsPathMappingsSaveLabel" => acpT("Save mappings"),
    "zfsPathMappingsSaveFailedTitle" => acpT("ZFS path mappings failed"),
    "zfsPathMappingsSaveFailedMessage" => acpT("The ZFS path mappings could not be saved right now."),
    "zfsPathMappingsBrowserTitle" => acpT("Folder picker"),
    "auditHistoryTitle" => acpT("Audit history"),
    "auditHistoryEntriesLabel" => acpT("entries available"),
    "auditHistoryOpenLabel" => acpT("Show history"),
    "auditHistoryLoadingMessage" => acpT("Loading cleanup history."),
    "auditHistoryEmptySummary" => acpT("No cleanup history has been recorded yet."),
    "auditHistoryEmptyMessage" => acpT("No cleanup, restore, or purge actions have been recorded yet."),
    "auditHistorySearchLabel" => acpT("Search history"),
    "auditHistorySearchPlaceholder" => acpT("Filter by path, action, or result..."),
    "auditHistoryNoMatchesMessage" => acpT("No history entries match that filter."),
    "auditHistoryCopySummaryLabel" => acpT("Copy summary"),
    "auditHistoryCopyFailedTitle" => acpT("Audit summary failed"),
    "auditHistoryCopyFailedMessage" => acpT("The audit summary could not be copied right now."),
    "auditHistoryMorePathsLabel" => acpT("more paths"),
    "auditRequestedLabel" => acpT("items submitted"),
    "quarantineManagerTitle" => acpT("Quarantine manager"),
    "quarantineManagerOpenLabel" => acpT("Show quarantine"),
    "quarantineLoadingLabel" => acpT("Loading quarantine"),
    "quarantineRefreshLabel" => acpT("Refresh"),
    "quarantineCountSingular" => acpT("quarantined folder"),
    "quarantineCountPlural" => acpT("quarantined folders"),
    "quarantineSummaryEmpty" => acpT("No quarantined folders are tracked right now."),
    "quarantineEmptyMessage" => acpT("Tracked quarantine entries will appear here once folders are moved into quarantine."),
    "quarantineLoadingMessage" => acpT("Reviewing tracked quarantined folders."),
    "quarantineSourceLabel" => acpT("Original"),
    "quarantineLocationLabel" => acpT("Quarantine path"),
    "quarantinePurgeScheduleDaysLabel" => acpT("Default purge in days"),
    "quarantinePurgeScheduleAtLabel" => acpT("Set purge at"),
    "quarantinePurgeScheduleSetLabel" => acpT("Set purge"),
    "quarantinePurgeScheduleClearLabel" => acpT("Clear purge timer"),
    "quarantinePurgeScheduleInvalidMessage" => acpT("Enter a future purge date and time."),
    "quarantineDefaultPurgeDaysInvalidMessage" => acpT("Enter a whole number of days from 0 to 3650."),
    "quarantineDefaultPurgeDaysFailedTitle" => acpT("Default purge setting failed"),
    "quarantineDefaultPurgeDaysFailedMessage" => acpT("The default quarantine purge timer could not be updated right now."),
    "quarantinePurgeScheduleFailedTitle" => acpT("Purge timer update failed"),
    "quarantinePurgeScheduleFailedMessage" => acpT("The purge timer could not be updated right now."),
    "quarantinePurgeScheduledLabel" => acpT("Scheduled purge"),
    "quarantineRestoreActionLabel" => acpT("Restore"),
    "restoreSelectedLabel" => acpT("Restore selected"),
    "quarantineRestoreConfirmTitle" => acpT("Restore quarantined folder?"),
    "quarantineRestoreConfirmManyTitle" => acpT("Restore selected quarantined folders?"),
    "quarantineRestoreConfirmMessage" => acpT("Restore this quarantined folder back to its original location."),
    "quarantineRestoreConfirmManyMessage" => acpT("Restore the selected quarantined folders back to their original locations."),
    "quarantineRestoreCheckingTitle" => acpT("Checking restore paths"),
    "quarantineRestoreCheckingMessage" => acpT("Checking whether the original restore paths are still available."),
    "quarantineRestoreConflictTitle" => acpT("Restore conflicts found"),
    "quarantineRestoreConflictLead" => acpT("One or more original restore paths already exist. Choose how to handle the conflicts."),
    "quarantineRestoreConflictListTitle" => acpT("Conflicting restore paths"),
    "quarantineRestoreConflictPathLabel" => acpT("Original path"),
    "quarantineRestoreConflictParentLabel" => acpT("Restore parent"),
    "quarantineRestoreConflictNameLabel" => acpT("Edited restore name"),
    "quarantineRestoreConflictEditedLabel" => acpT("Edited restore path"),
    "quarantineRestoreConflictSuggestedLabel" => acpT("Suggested suffix restore"),
    "quarantineRestoreConflictSkipLabel" => acpT("Skip conflicts"),
    "quarantineRestoreConflictSuffixLabel" => acpT("Restore edited names"),
    "quarantineRestoreConflictReviewLabel" => acpT("Review conflict"),
    "quarantineRestoreConflictNameHint" => acpT("Edit only the restored folder name. The parent path stays fixed."),
    "quarantineRestoreConflictInvalidMessage" => acpT("Enter a restore name for every conflicting folder. Names cannot contain slashes."),
    "quarantineRestoreConflictSkipMessage" => acpT("Conflicting folders will stay in quarantine and the other selected folders will restore normally."),
    "quarantineRestoreConflictSuffixMessage" => acpT("Conflicting folders will restore beside the existing folders using the edited names below."),
    "quarantineRestoreListTitle" => acpT("Folder to restore"),
    "quarantineRestoreListManyTitle" => acpT("Folders to restore"),
    "quarantineRestoreLoadingTitle" => acpT("Restoring quarantined folders"),
    "quarantineRestoreLoadingMessage" => acpT("Moving selected folders back to their original locations."),
    "quarantineRestoreSuccessTitle" => acpT("Restore complete"),
    "quarantineRestoreWarningTitle" => acpT("Restore finished with warnings"),
    "quarantineRestoreFailedTitle" => acpT("Restore failed"),
    "quarantinePurgeActionLabel" => acpT("Purge"),
    "purgeSelectedLabel" => acpT("Purge selected"),
    "quarantinePurgeConfirmTitle" => acpT("Purge quarantined folder?"),
    "quarantinePurgeConfirmManyTitle" => acpT("Purge selected quarantined folders?"),
    "quarantinePurgeConfirmMessage" => acpT("Permanently delete this quarantined folder."),
    "quarantinePurgeConfirmManyMessage" => acpT("Permanently delete the selected quarantined folders."),
    "quarantinePurgeCheckboxConfirmLabel" => acpT("I confirm I want to permanently delete the selected quarantined folders. This action cannot be undone by this plugin."),
    "quarantinePurgeListTitle" => acpT("Folder to purge"),
    "quarantinePurgeListManyTitle" => acpT("Folders to purge"),
    "quarantinePurgeLoadingTitle" => acpT("Purging quarantined folders"),
    "quarantinePurgeLoadingMessage" => acpT("Permanently deleting selected quarantined folders."),
    "quarantinePurgeSuccessTitle" => acpT("Purge complete"),
    "quarantinePurgeWarningTitle" => acpT("Purge finished with warnings"),
    "quarantinePurgeFailedTitle" => acpT("Purge failed"),
    "quarantineManagerSelectionEmptyMessage" => acpT("Select at least one quarantined folder before continuing."),
    "quarantineManagerActionFailedMessage" => acpT("The quarantine manager action could not be completed right now."),
    "quarantineActionLabel" => acpT("Quarantine selected"),
    "deleteActionLabel" => acpT("Delete selected"),
    "quarantineListTitle" => acpT("Folders to quarantine"),
    "quarantineDetailMessage" => acpT("The selected folders will be moved into the quarantine root instead of being permanently deleted."),
    "previewListTitle" => acpT("Operation preview"),
    "previewReadyLabel" => acpT("Ready"),
    "resultDeletedLabel" => acpT("Deleted"),
    "resultQuarantinedLabel" => acpT("Quarantined"),
    "resultRestoredLabel" => acpT("Restored"),
    "resultPurgedLabel" => acpT("Purged"),
    "resultSkippedLabel" => acpT("Skipped"),
    "resultConflictLabel" => acpT("Conflict"),
    "resultBlockedLabel" => acpT("Locked"),
    "resultMissingLabel" => acpT("Missing"),
    "resultErrorLabel" => acpT("Error")
  )
);
?>
<script>
window.AppdataCleanupPlusHostThemeName = <?php echo json_encode($hostThemeName, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE); ?>;
(function applyAppdataCleanupPlusHostTheme(rawThemeName) {
  const safeThemeName = String(rawThemeName || '').trim().toLowerCase();
  if (!safeThemeName) {
    return;
  }
  const applyAttrs = () => {
    try {
      document.documentElement?.setAttribute('data-acp-host-theme', safeThemeName);
      document.body?.setAttribute('data-acp-host-theme', safeThemeName);
    } catch (_error) {}
  };
  applyAttrs();
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', applyAttrs, { once: true });
  }
})(window.AppdataCleanupPlusHostThemeName);
</script>
<link rel="stylesheet" href="/plugins/appdata.cleanup.plus/styles/appdata.cleanup.plus.css?v=<?php echo $cssVersion; ?>">

<div class="acp-shell" id="acp-app" lang="<?php echo htmlspecialchars($config['languageTag'], ENT_QUOTES); ?>" dir="<?php echo $config['direction']; ?>" data-acp-host-theme="<?php echo htmlspecialchars($hostThemeName, ENT_QUOTES); ?>"<?php if ( $hostThemeClass ) { ?> data-acp-theme-class="<?php echo htmlspecialchars($hostThemeClass, ENT_QUOTES); ?>"<?php } ?>>
  <header class="acp-hero">
    <div class="acp-brand-mark">
      <img src="/plugins/appdata.cleanup.plus/images/appdata.cleanup.plus.png" alt="">
    </div>
    <div class="acp-hero-copy">
      <h2 class="acp-title"><?php echo acpH("Appdata Cleanup Plus"); ?></h2>
      <p class="acp-subtitle"><?php echo acpH("Review orphaned appdata before delete"); ?></p>
    </div>
    <div class="acp-header-actions">
      <button type="button" class="acp-button acp-button-secondary" id="acp-rescan"><?php echo acpH("Rescan"); ?></button>
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" id="acp-help">
        <span class="acp-button-icon acp-button-help-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <circle cx="12" cy="12" r="9"></circle>
            <path d="M9.75 9.25a2.35 2.35 0 0 1 4.5 1c0 1.7-2.25 1.9-2.25 3.45"></path>
            <path d="M12 16.8h.01"></path>
          </svg>
        </span>
        <span><?php echo acpH("Help"); ?></span>
      </button>
    </div>
  </header>

  <section class="acp-dashboard-top">
    <section class="acp-summary-grid" id="acp-summary-cards" aria-live="polite"></section>
    <section class="acp-mode-strip" id="acp-mode-strip" aria-live="polite"></section>
  </section>

  <section class="acp-toolbar acp-toolbar-panel acp-results-controls-panel">
    <div class="acp-toolbar-inline">
      <label class="acp-field acp-field-grow">
        <span class="acp-field-label"><?php echo acpH("Search"); ?></span>
        <input type="search" id="acp-search" class="acp-input" placeholder="<?php echo acpH("Filter by app name or path..."); ?>" autocomplete="off">
      </label>

      <label class="acp-field acp-compact-field acp-sort-field">
        <span class="acp-field-label"><?php echo acpH("Sort by"); ?></span>
        <select id="acp-sort" class="acp-select">
          <option value="name" selected><?php echo acpH("Name"); ?></option>
          <option value="path"><?php echo acpH("Path"); ?></option>
          <option value="last-used"><?php echo acpH("Last used"); ?></option>
          <option value="size"><?php echo acpH("Size: Largest"); ?></option>
          <option value="source"><?php echo acpH("Source"); ?></option>
          <option value="type"><?php echo acpH("Type"); ?></option>
          <option value="risk"><?php echo acpH("Risk: Highest"); ?></option>
        </select>
      </label>
    </div>
  </section>

  <section class="acp-action-toolbar" id="acp-action-toolbar">
    <div class="acp-action-toolbar-copy">
      <strong><?php echo acpH("Action bar"); ?></strong>
      <span><?php echo acpH("No items selected"); ?></span>
    </div>
    <div class="acp-action-toolbar-actions">
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" data-action="open-appdata-sources">
        <span class="acp-button-icon acp-button-dataset-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="M5 6.75c0-1.24 3.13-2.25 7-2.25s7 1.01 7 2.25v10.5c0 1.24-3.13 2.25-7 2.25s-7-1.01-7-2.25Z"></path>
            <path d="M5 6.75c0 1.24 3.13 2.25 7 2.25s7-1.01 7-2.25"></path>
            <path d="M5 12c0 1.24 3.13 2.25 7 2.25s7-1.01 7-2.25"></path>
            <path d="M5 17.25c0 1.24 3.13 2.25 7 2.25s7-1.01 7-2.25"></path>
          </svg>
        </span>
        <span class="acp-button-label acp-button-accent-label"><?php echo acpH("Appdata Sources"); ?></span>
      </button>
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" data-action="toggle-quarantine">
        <span class="acp-button-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="M12 3.5 19 6.25v5.25c0 4.4-2.75 7.5-7 9-4.25-1.5-7-4.6-7-9V6.25Z"></path>
          </svg>
        </span>
        <span><?php echo acpH("Show Quarantine"); ?></span>
      </button>
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" data-action="open-audit-history">
        <span class="acp-button-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="M3 12a9 9 0 1 0 3-6.7"></path>
            <path d="M3 4.5v5h5"></path>
            <path d="M12 7.5v5l3.25 1.9"></path>
          </svg>
        </span>
        <span><?php echo acpH("History"); ?></span>
      </button>
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" data-action="open-tools">
        <span class="acp-button-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="M14.7 6.3a4 4 0 0 0-5 5L4 17v3h3l5.7-5.7a4 4 0 0 0 5-5l-2.6 2.6-2.9-2.9Z"></path>
          </svg>
        </span>
        <span><?php echo acpH("Tools"); ?></span>
        <span class="acp-button-icon acp-button-chevron-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="m7 10 5 5 5-5"></path>
          </svg>
        </span>
      </button>
    </div>
  </section>

  <section class="acp-notices" id="acp-notices"></section>
  <section class="acp-results" id="acp-results" aria-live="polite"></section>

  <section class="acp-bottom-bar" id="acp-bottom-bar">
    <span class="acp-bottom-selection-icon" aria-hidden="true"></span>
    <div class="acp-bottom-copy">
      <strong id="acp-selection-summary"><?php echo htmlspecialchars(acpP("{count} folders selected", 0), ENT_QUOTES, 'UTF-8'); ?></strong>
      <span id="acp-selection-detail"><?php echo acpH("Select ready rows to clean them. Protected paths stay blocked."); ?></span>
    </div>
    <div class="acp-bottom-actions">
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" id="acp-select-all" disabled>
        <span class="acp-button-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <circle cx="12" cy="12" r="9"></circle>
            <path d="m8.25 12 2.45 2.45 5.05-5.3"></path>
          </svg>
        </span>
        <span class="acp-button-label"><?php echo acpH("Select ready"); ?></span>
      </button>
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" id="acp-clear-selection" disabled>
        <span class="acp-button-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <circle cx="12" cy="12" r="9"></circle>
            <path d="m9 9 6 6"></path>
            <path d="m15 9-6 6"></path>
          </svg>
        </span>
        <span class="acp-button-label"><?php echo acpH("Clear selection"); ?></span>
      </button>
      <button type="button" class="acp-button acp-button-secondary acp-button-warning acp-button-with-icon" id="acp-dry-run" disabled>
        <span class="acp-button-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="M8 5.5v13l10-6.5Z"></path>
          </svg>
        </span>
        <span class="acp-button-label"><?php echo acpH("Dry run"); ?></span>
      </button>
      <button type="button" class="acp-button acp-button-secondary acp-button-with-icon" id="acp-primary-action" disabled>
        <span class="acp-button-icon acp-primary-action-icon acp-primary-quarantine-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <path d="M12 3.5 19 6.25v5.25c0 4.4-2.75 7.5-7 9-4.25-1.5-7-4.6-7-9V6.25Z"></path>
          </svg>
        </span>
        <span class="acp-button-icon acp-primary-action-icon acp-primary-delete-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" focusable="false">
            <rect x="6.5" y="10.5" width="11" height="8" rx="1.75"></rect>
            <path d="M8.5 10.5V8.25a3.5 3.5 0 0 1 7 0v2.25"></path>
          </svg>
        </span>
        <span class="acp-button-label"><?php echo acpH("Quarantine selected"); ?></span>
      </button>
    </div>
  </section>
</div>

<script>
window.appdataCleanupPlusConfig = <?php echo json_encode($config, JSON_UNESCAPED_SLASHES | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT); ?>;
</script>
<script src="/plugins/appdata.cleanup.plus/scripts/appdata.cleanup.plus.shared.js?v=<?php echo $jsVersion; ?>"></script>
<script src="/plugins/appdata.cleanup.plus/scripts/appdata.cleanup.plus.panels.js?v=<?php echo $jsVersion; ?>"></script>
<script src="/plugins/appdata.cleanup.plus/scripts/appdata.cleanup.plus.js?v=<?php echo $jsVersion; ?>"></script>
